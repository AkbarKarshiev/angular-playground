import { TestProjectionDirective } from './test-projection.directive';

describe('TestProjectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TestProjectionDirective();
    expect(directive).toBeTruthy();
  });
});
